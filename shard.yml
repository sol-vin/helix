name: helix
version: 0.1.0

authors:
  - Ian Rash <ian@sol.vin>

targets:
  helix:
    main: src/helix.cr
  sandbox:
    main: src/sandbox.cr
  sandbox2:
    main: src/sandbox2.cr

dependencies:
  raylib-cr:
    github: sol-vin/raylib-cr
    branch: master
crystal: '>= 1.13.2'

license: MIT
